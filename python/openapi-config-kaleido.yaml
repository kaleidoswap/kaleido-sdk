# OpenAPI Generator configuration for Kaleidoswap API (Python)
# See: https://github.com/openapi-generators/openapi-python-client

project_name_override: "kaleidoswap_rgb_lsp_api_client"
package_name_override: "kaleido"
package_version_override: "0.1.5"

# Use httpx for async support
http_timeout: 30
use_path_prefixes_for_title_model_names: false

