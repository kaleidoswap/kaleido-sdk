import { RetryStrategy } from '../types/errors';
export interface RetryConfig {
    maxRetries: number;
    initialDelay: number;
    maxDelay: number;
    exponentialBase: number;
    jitter: boolean;
    retryOnExceptions: Array<new (...args: any[]) => Error>;
    respectErrorRetryConfig?: boolean;
    customRetryStrategy?: RetryStrategy;
}
export declare const defaultRetryConfig: RetryConfig;
/**
 * Enhanced retry function that works with the new error system
 */
export declare function retry<T>(fn: () => Promise<T>, config?: Partial<RetryConfig>): Promise<T>;
/**
 * Convenient wrapper for retry with predefined configuration
 */
export declare const withRetry: <T>(fn: () => Promise<T>, config?: Partial<RetryConfig>) => Promise<T>;
/**
 * Creates a retry wrapper for a function
 */
export declare const createRetryWrapper: <TArgs extends any[], TReturn>(fn: (...args: TArgs) => Promise<TReturn>, config?: Partial<RetryConfig>) => (...args: TArgs) => Promise<TReturn>;
