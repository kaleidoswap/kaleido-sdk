import { HttpClientConfig } from './http/client';
import { Swap, SwapRequest, SwapResponse, AssetResponse, PairResponse, PairQuoteResponse, ConfirmSwapRequest, ConfirmSwapResponse, GetInfoResponseModel, NetworkInfoResponse } from './index';
export interface KaleidoConfig extends Omit<HttpClientConfig, 'baseUrl'> {
    baseUrl?: string;
    nodeUrl?: string;
    wsUrl?: string;
}
export declare class KaleidoClient {
    private readonly apiClient;
    private readonly nodeClient;
    private readonly wsClient;
    constructor(config: KaleidoConfig);
    private ensureNodeClient;
    getLspInfo(): Promise<GetInfoResponseModel>;
    getLspConnectionUrl(): Promise<string>;
    getLspNetworkInfo(): Promise<NetworkInfoResponse>;
    connectPeer(connectionUrl: string): Promise<any>;
    getAssetMetadata(assetId: string): Promise<any>;
    getNodeInfo(): Promise<{
        pubkey: string;
    }>;
    getNodePubkey(): Promise<string>;
    assetList(): Promise<AssetResponse>;
    pairList(): Promise<PairResponse>;
    quoteRequest(fromAsset: string, toAsset: string, fromAmount?: number, toAmount?: number): Promise<PairQuoteResponse>;
    quoteRequestWS(fromAsset: string, toAsset: string, fromAmount?: number, toAmount?: number): Promise<PairQuoteResponse>;
    initMakerSwap(request: SwapRequest): Promise<SwapResponse>;
    executeMakerSwap(request: ConfirmSwapRequest): Promise<ConfirmSwapResponse>;
    whitelistTrade(swapstring: string): Promise<Record<string, never>>;
    atomicSwapStatus(orderId: any): Promise<Swap>;
    waitForSwapCompletion(paymentHash: string, timeoutSeconds?: number, pollIntervalSeconds?: number): Promise<Swap>;
    createOrder(request: object): Promise<any>;
    swapOrderStatus(request: string): Promise<any>;
    swapOrderAnalytic(): Promise<any>;
}
